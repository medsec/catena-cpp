#include "components/gamma/gamma_algorithm.h"

namespace catena {
namespace gamma {
  void *GammaAlgorithm::Execute(
      const uint8_t g,
      uint8_t *v,
      uint8_t *salt,
      const size_t salt_len) {
    return NULL_PTR;
  }
}  // namespace gamma
}  // namespace catena

